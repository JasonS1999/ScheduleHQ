name: schedulehq_android
description: "ScheduleHQ Android - Employee Schedule Viewer App"
publish_to: 'none'
version: 2.7.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.13.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.6
  firebase_messaging: ^15.2.5
  
  # UI
  intl: ^0.20.2
  rxdart: ^0.28.0
  
  # Shared models
  schedule_shared:
    path: ../packages/schedule_shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
